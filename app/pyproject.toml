[project]
name = "myapp-ab"
version = "0.1.0"
description = "Простой A/B сервис для canary-тестов"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
]